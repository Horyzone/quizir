services:
  postgresql:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: quizir
      POSTGRES_PASSWORD: quizir
      POSTGRES_DB: quizir_dev
    volumes:
      - $PWD/quizir_data:/var/lib/postgresql/data
    ports:
      - "127.0.0.1:5432:5432"